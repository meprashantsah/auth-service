spring:
  datasource:
    url: jdbc:tc:postgresql:16:///auth_db?TC_REUSABLE=true
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
  threads:
    virtual:
      enabled: true

jwt:
  keys:
    generate-on-missing: true

internal:
  api-key: test-internal-key

logging:
  level:
    org.testcontainers: INFO